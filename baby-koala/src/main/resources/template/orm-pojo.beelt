package ${koalaTG.packageName}.domian;

  <%
    for(importClass in koalaTG.importClasses){
    print("import "+importClass+";\n");
    }
  %>
/**
 * @author edliao on Auto generate.
 */
@lombok.Data
public class ${koalaTG.pojoName} {
  <%
    var size = koalaTG.fieldClassNames.~size;
    for(var i=0;i<size;i++){
      print("\t"+koalaTG.fieldClassNames[i]+" "+koalaTG.fieldNames[i]+";\n");
    }
  %>
}